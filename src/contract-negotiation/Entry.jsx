import React from 'react';
import { Switch, Route } from 'react-router-dom';

export default function Entry(props) {
    return (
        <Switch>
            <Route>

            </Route>
        </Switch>
    )
}